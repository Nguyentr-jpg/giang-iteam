# 2026-07

## 2026-07-01 (run)
- Đọc: 1 task To Do (raw, unassigned)
- Phase 0:
  - Lên plan (chờ duyệt): 1 — task `bytq22hnbt4o` "RF agent tự cập nhật". Đọc code
    retask/agent/auto_updater.py (đã đúng hành vi mong muốn) vs
    reflow/agent/agent_updater.py (chỉ notify, cần user tự bấm tray mới cài) →
    đề xuất chỉ sửa reflow, giao `duc`. Đã ping Slack kèm plan, chờ founder duyệt.
  - Hỏi founder: 0
  - Vừa duyệt → tạo task con: 0
- Phase 1: không có task đã gán (assignee ∈ roster) ở trạng thái To Do — bỏ qua.
- Lỗi: 0

## 2026-07-01 (correction — cùng ngày, sau feedback founder)
- Founder chỉ ra: "RF Agent" là tên riêng của **retask** (không phải reflow —
  cái đó tên "ReFlow Agent"). Nhận nhầm repo ở lần đọc trước.
- Đọc lại `retask/agent/main.py`: đã gọi `register_autostart()` + `start_background_poll()`
  lúc khởi động → tưởng "đã có sẵn". Đổi task sang `need_info`, hỏi lại founder.

## 2026-07-01 (điều tra sâu + thực thi — sau khi founder nói "vẫn không hoạt động")
- Founder làm rõ: RF Agent là **local agent dùng chung retask+reflow**, chỉ nằm
  trong repo retask. Yêu cầu thật: có bản mới → **popup hỏi Yes/No** → bấm Yes là cập nhật.
- **Root cause tìm ra (đọc code + so bản cũ):** `retask/agent/auto_updater.py`
  chạy installer bằng `subprocess.Popen([out,"/S"])` (CreateProcess thường) nhưng
  `installer.nsi` có `RequestExecutionLevel admin`; agent chạy quyền user thường
  (autostart HKCU\Run) → **WinError 740 "requires elevation"**, installer không
  chạy, lỗi bị try/except nuốt im lặng. Ngoài ra không hề có popup hỏi user.
  Bản desktop CŨ (`reflow/update_checker.py`) từng chạy được vì có
  `messagebox.askyesno` + `Start-Process -Verb RunAs` — bản RF Agent mới bỏ mất cả 2.
- Phụ: row `app_updates` (RFAgent) kẹt ở 1.0.25 từ 27/5; version.json repo = 1.0.23
  (thấp hơn) → build test phải ≥ 1.0.26.
- Founder duyệt trong chat → tạo task con `d7k2m9x4qp1w` (assignee `duc`, repo retask,
  High), mark yêu cầu gốc `bytq22hnbt4o` = spawned + In Progress.
- Phase 1: spawn subagent Đức → sửa `agent/auto_updater.py` (popup Yes/No marshal
  về main Tk thread + `ShellExecuteW runas` fix elevation + nhớ version "No").
  Mở **PR #76** (branch giang/d7k2m9x4qp1w). Giang review diff: sạch, 1 file, +157/-12,
  không đụng DB/secret/version.json. Task → In Review.

## 2026-07-01 (QC review + merge — PR #76)
- Gemini Code Assist bot review PR #76, bắt **3 lỗi thật** (đều xác nhận đúng):
  1. `import main` nạp lại module lần 2 → `_tk_root` luôn None → popup luôn rơi vào
     fallback `Tk()` trong poll thread (nguy hiểm). Fix: `sys.modules.get("__main__")`.
  2. Fallback PowerShell trong `_launch_installer_elevated` gây double-UAC + trả True
     giả → `os._exit(0)` giết agent oan. Fix: bỏ fallback, chỉ `ShellExecuteW runas`,
     thất bại → False (agent sống, hỏi lại vòng sau).
  3. `start_background_poll` chạy cả trên Mac tải .exe vô ích. Fix: guard `sys.platform` win-only.
- Giang tự áp cả 3 (nhỏ, chắc chắn) → commit `7586c07`, push cùng branch, `py_compile` pass.
  Comment tổng kết trên PR. Task giữ In Review (founder đóng).
- **Founder merged PR #76** → code vào main retask. Unsubscribe PR. Đã nhắc founder
  bước cuối: tag `agent-v1.0.26` (≥1.0.26 vì DB 1.0.25) để build + tới máy user.
- Lỗi: 0.

## 2026-07-01 (run — routine mới)
- Đọc: 1 task To Do (raw, unassigned) — `o4vlyyjyj0np` "quét thư mục done báo
  RealFaster gửi mail khách tự nhiên không hoạt động khi đơn kéo dài qua ngày".
- Phase 0:
  - Điều tra 2 vòng (Explore agent) để xác định đúng feature: lần đầu lạc vào
    `csagent/missing_upload.py` (nhắc nội bộ thiếu upload — SAI feature); lần 2
    tìm đúng `csagent/delivery_notify.py` Phase 7 "báo đơn xong" (quét folder
    done → ghi cờ `production_delivery` → RealFaster cron gửi mail khách).
  - Đọc trực tiếp `delivery_notify.py`: nghi vấn `_resolve_cutoff()` (dòng 98-121)
    chốt mốc chặn ngày CHỈ 1 lần lúc Phase 7 chạy lần đầu (persist `agent_meta`);
    nếu agent restart mất state, cutoff có thể bị chốt lại sai → đơn làm dở qua
    ngày không được báo. Chưa xác nhận 100% — giao Đức tự verify trước khi sửa.
  - Lên plan (chờ duyệt): 1 subtask giao `duc`, repo `cs_agent`. Nhấn mạnh: giữ
    nguyên cơ chế chặn spam mail đơn cũ (precedent 24/4 RealFaster — 300+ mail
    nhầm), và nếu phải đụng phía RealFaster (cron gửi mail) thì DỪNG xin duyệt
    riêng theo RULE 1.5 của RealFaster. Đã ping Slack kèm plan, chờ founder duyệt.
  - Hỏi founder: 0
  - Vừa duyệt → tạo task con: 0
- Phase 1: không có task đã gán (assignee ∈ roster) ở trạng thái To Do — bỏ qua.
- Lỗi: 0.
